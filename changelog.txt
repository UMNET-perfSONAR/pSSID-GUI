- yaml file format now {type: [name: testname, param1: true]}
- meta.yml is a separate file
- Global yaml files now stored in group_vars/all
- Added tasks for groups
- You can now edit tasks and metadata for the all group
- Added metadata for tests and archivers

- Added number formatting for signal strengths in ssid profiles
- Added error checking for giving the same name to multiple items
- Fixed minor typos in form placeholders
- Form inputs that can only receive number inputs now output numbers instead of strings
- Checkboxes now default to false instead of undefined (which used to block form submits because they were required)
- Fixed a lot of small bugs
